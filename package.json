{
  "name": "ios-imap-notes",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:xpi": "bash scripts/build-xpi.sh",
    "check": "node --check background.js && node --check experiments/notesHeader/implementation.js && node --check message-display/editor.js && node --check scripts/apple-note.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
